// import type { FollowUpData } from "src/interfaces/app_interfaces";

export default class FollowUpServise {

  getFollowUpListByUserId (userId: number) {

    console.log('Hello from follow up service', userId);
  }
}